services:
  - type: web
    name: razorpay-charger
    runtime: python
    start_command: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 8 --timeout 0
    env_vars:
      - key: PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH
        value: /usr/bin/google-chrome-stable
